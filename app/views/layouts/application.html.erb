<!DOCTYPE html>
<html>
  <head>
    <title>RailsSimpleAirbnb</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="d-flex justify-content-between py-3 px-5 bg-light">
      <%= link_to "all flats", flats_path, class: 'text-decoration-none text-secondary'  %>

      <div class="d-flex">
        <form action="/flats" class="form-inline">
          <input type="text" name="search" value="<%= @query %>" class="form-control">
          <input type="submit" class="btn btn-success">
        </form>

        <%= link_to "add a new flat", new_flat_path, class: 'text-decoration-none text-secondary'  %>

      </div>
    </div>

    <div class="container">

      <%= yield %>

    </div>


  </body>
</html>
